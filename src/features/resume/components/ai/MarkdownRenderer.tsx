'use client';

// Moved to src/shared/components/MarkdownRenderer.tsx (now shared by resume and aiAssistant features).
// Re-exported here so existing imports in this feature keep working.
export { MarkdownRenderer } from '@/shared/components/MarkdownRenderer';
